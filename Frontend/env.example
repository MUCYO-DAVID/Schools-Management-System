# Copy to Frontend/.env.local for local dev
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000

# Vercel production (frontend only) — set in Project → Environment Variables:
# NEXT_PUBLIC_BACKEND_URL=https://rwandaschoolsbridgesystem.onrender.com
# Vercel Root Directory must be: Frontend

# Backend env vars go on Render (see Backend/env.example):
# DATABASE_URL, JWT_SECRET, GROQ_API_KEY, EMAIL_SERVICE, EMAIL_USER, EMAIL_PASSWORD, EMAIL_FROM
