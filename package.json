{
  "name": "schools-management-system",
  "private": true,
  "description": "RSBS monorepo — Frontend (Next.js) + Backend (Express on Vercel serverless)",
  "scripts": {
    "dev:frontend": "npm run dev --prefix Frontend",
    "dev:backend": "npm run dev --prefix Backend",
    "build:frontend": "npm run build --prefix Frontend"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "serverless-http": "^3.2.0"
  }
}
